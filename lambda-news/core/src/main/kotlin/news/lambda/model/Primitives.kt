package news.lambda.model

// TODO: inline class when Compose is final
data class UnixTime(val millis: Long)

// TODO: inline class when Compose is final
data class ItemId(val value: Long)

// TODO: inline class when Compose is final
data class UserId(val value: String)

